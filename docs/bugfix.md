The phrasing on multiple pages is slightly confusing or misleading. In some cases different words would suit the purpose more.
Fix UX on schedule session dialog. The "Until" time is not aligned with "starting".  Fix it
