{
  "name": "@tmi/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker API + static host for the TMI tutor management portal",
  "scripts": {
    "predev": "node -e \"require('node:fs').mkdirSync('../web/dist',{recursive:true})\"",
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "db:create": "wrangler d1 create tmi-portal-db",
    "db:rebuild": "wrangler d1 execute tmi-portal-db --local --file=./db/schema.sql",
    "db:rebuild:remote": "wrangler d1 execute tmi-portal-db --remote --file=./db/schema.sql",
    "db:seed": "wrangler d1 execute tmi-portal-db --local --file=./db/seed.sql",
    "db:reset": "npm run db:rebuild && npm run db:seed",
    "db:studio": "wrangler d1 execute tmi-portal-db --local --command=\"SELECT id, email, full_name, role, status, last_login_at, deleted_at FROM users ORDER BY full_name\""
  },
  "dependencies": {
    "@tmi/shared": "*",
    "hono": "^4.13.8",
    "jose": "^6.2.12",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260919.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.135.0"
  }
}
